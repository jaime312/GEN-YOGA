name: Deploy iOS App to App Store Connect

on:
  workflow_dispatch:
    inputs:
      notes:
        description: 'Release notes / description'
        required: false
        default: 'Release v6.42: Unified classes, teacher profiles, and Farmacia Q19 alliance'

jobs:
  build-and-deploy:
    runs-on: macos-14
    timeout-minutes: 45

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'
          cache-dependency-path: 'app ios/package-lock.json'

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode_15.4.app

      - name: Install dependencies & Sync Capacitor
        run: |
          cd "app ios"
          npm install
          npx cap sync ios

      - name: Setup App Store Connect API Key
        env:
          KEY_ID: ${{ secrets.APP_STORE_CONNECT_KEY_ID }}
          ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_ISSUER_ID }}
          PRIVATE_KEY: ${{ secrets.APP_STORE_CONNECT_PRIVATE_KEY }}
        run: |
          python3 - << 'EOF'
          import os

          fallback_key = """-----BEGIN PRIVATE KEY-----
          MIGTAgEAMBMGByqGSM49AgEGCCqGSM49AwEHBHkwdwIBAQQgl6OMfUJRhY9aZxhp
          gHpgG8CTkyFT5noJCegG9MSseaigCgYIKoZIzj0DAQehRANCAASvfzh7wmSLZhA9
          uTeKh54wgdgz+SkwY9xehFIB6wly3Hhl6zb435jmudLxqQTih8Uud2kMoG1pKiFK
          Lr6MJ4AZ
          -----END PRIVATE KEY-----"""

          raw_key = os.environ.get("PRIVATE_KEY", "").strip()
          if raw_key and "-----BEGIN" in raw_key:
              key_to_use = raw_key
          else:
              key_to_use = "\n".join([line.strip() for line in fallback_key.splitlines()])

          key_to_use = key_to_use.replace("\r\n", "\n").strip() + "\n"

          paths = [
              os.path.expanduser("~/.appstoreconnect/private_keys/AuthKey_9T2974ST9M.p8"),
              os.path.expanduser("~/.private_keys/AuthKey_9T2974ST9M.p8")
          ]

          for p in paths:
              os.makedirs(os.path.dirname(p), exist_ok=True)
              with open(p, "w", encoding="utf-8", newline="\n") as f:
                  f.write(key_to_use)
              os.chmod(p, 0o600)
          print("App Store Connect API key configured successfully")
          EOF

      - name: Build & Archive iOS App
        env:
          APP_STORE_CONNECT_KEY_ID: ${{ secrets.APP_STORE_CONNECT_KEY_ID }}
          APP_STORE_CONNECT_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_ISSUER_ID }}
        run: |
          KEY_ID="${APP_STORE_CONNECT_KEY_ID:-9T2974ST9M}"
          ISSUER_ID="${APP_STORE_CONNECT_ISSUER_ID:-c8f0f943-872c-4153-b89a-86fb7cc78b8f}"
          TEAM_ID="YLP3L4G7F3"
          KEY_PATH="$HOME/.appstoreconnect/private_keys/AuthKey_${KEY_ID}.p8"
          
          cd "app ios/ios/App"
          
          echo "Building and signing archive for Team ID: $TEAM_ID"
          
          xcodebuild \
            -project App.xcodeproj \
            -scheme App \
            -configuration Release \
            -destination "generic/platform=iOS" \
            -archivePath "$PWD/build/App.xcarchive" \
            -allowProvisioningUpdates \
            -authenticationKeyID "$KEY_ID" \
            -authenticationKeyIssuerID "$ISSUER_ID" \
            -authenticationKeyPath "$KEY_PATH" \
            DEVELOPMENT_TEAM="$TEAM_ID" \
            archive

      - name: Export IPA & Upload to App Store Connect
        env:
          APP_STORE_CONNECT_KEY_ID: ${{ secrets.APP_STORE_CONNECT_KEY_ID }}
          APP_STORE_CONNECT_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_ISSUER_ID }}
        run: |
          KEY_ID="${APP_STORE_CONNECT_KEY_ID:-9T2974ST9M}"
          ISSUER_ID="${APP_STORE_CONNECT_ISSUER_ID:-c8f0f943-872c-4153-b89a-86fb7cc78b8f}"
          TEAM_ID="YLP3L4G7F3"
          KEY_PATH="$HOME/.appstoreconnect/private_keys/AuthKey_${KEY_ID}.p8"
          
          cd "app ios"
          
          xcodebuild \
            -exportArchive \
            -archivePath "ios/App/build/App.xcarchive" \
            -exportPath "build/ipa" \
            -exportOptionsPlist "exportOptions.plist" \
            -allowProvisioningUpdates \
            -authenticationKeyID "$KEY_ID" \
            -authenticationKeyIssuerID "$ISSUER_ID" \
            -authenticationKeyPath "$KEY_PATH"
          
          IPA_FILE=$(find build/ipa -name "*.ipa" | head -n 1)
          echo "Found IPA file: $IPA_FILE"
          
          xcrun altool --upload-app \
            --type ios \
            --file "$IPA_FILE" \
            --apiKey "$KEY_ID" \
            --apiIssuer "$ISSUER_ID"

      - name: Upload IPA Artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: GenYoga-iOS-v6.42
          path: app ios/build/ipa/*.ipa
          if-no-files-found: ignore
